server.port: 8080

spring:
  datasource:
    driverClassName: org.postgresql.Driver;
    url: jdbc:postgresql://db:5432/catsgram
    username: dbuser
    password: 12345
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init.mode: always